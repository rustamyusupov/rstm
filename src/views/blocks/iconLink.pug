mixin iconLink(href, icon, width=16, height=16, alt, external)
  a(
    href=href
    target=(external ? "_blank" : "")
    rel=(external ? "nofollow noopener" : "")
  )&attributes(attributes)
    img.icon(
      src="/icons/" + icon
      width=width
      height=height
      alt=alt
    )