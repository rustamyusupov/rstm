version: '3.8'

services:
  frontend:
    image: frontend
    build:
      context: ./frontend
    ports:
      - '80:80'
    networks:
      - frontend
      - backend

  backend:
    image: backend
    build:
      context: ./backend
    networks:
      - backend

networks:
  frontend:
  backend:
