{
  "name": "rstm",
  "version": "1.0.0",
  "description": "Personal stuff about me",
  "license": "MIT",
  "private": true,
  "author": "Rustam Yuspov",
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "NODE_ENV=production babel server --out-dir dist/server --source-maps inline",
    "start": "NODE_ENV=production node dist/server/bin/server.js",
    "dev": "nodemon --exec babel-node server/bin/server.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "fastify": "^3.25.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.16.7",
    "@babel/core": "^7.16.7",
    "@babel/node": "^7.16.7",
    "@babel/preset-env": "^7.16.7",
    "eslint": "^8.6.0",
    "nodemon": "^2.0.15"
  }
}
